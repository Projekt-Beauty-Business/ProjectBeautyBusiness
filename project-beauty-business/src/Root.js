import React from 'react';
import PropTypes from 'prop-types';
import Main from './views/Main/Main';

const Root = (props) => (
    <>
        <Main/>
    </>
);

Root.propTypes = {};

export default Root;
